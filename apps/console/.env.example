# @tagit/console environment

# WalletConnect (existing)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# tagit-agent dashboard (feature D2) — read-only Supabase access
# Anon key is safe to expose in the browser bundle (RLS allows SELECT only).
NEXT_PUBLIC_SUPABASE_URL=https://gdwzapbapqwpqlqnzxru.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
