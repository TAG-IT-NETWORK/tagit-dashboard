"use client";

export const dynamic = "force-dynamic";

import { AssemblyLineConsole } from "@/components/assembly-line-console";

export default function AssemblyLinePage() {
  return <AssemblyLineConsole />;
}
